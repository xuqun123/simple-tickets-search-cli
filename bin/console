#!/usr/bin/env ruby
# frozen_string_literal: true

# This is a ruby console to try out with different code features

require 'bundler/setup'
Dir['./lib/*.rb', './lib/*/*.rb'].sort.each { |file| require file }

require 'irb'
IRB.start(__FILE__)
