#!/usr/bin/env ruby
# frozen_string_literal: true

# This is a CLI script to run the search system
require 'pry'

Dir['./lib/*.rb', './lib/*/*.rb'].sort.each { |file| require file }

Search::Runner.process(ARGV)
